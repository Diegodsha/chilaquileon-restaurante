import backStyle from '../styles/background.module.css'

const menu = () => {
  return (
    <div  className={` ${backStyle.menu}`}>
      <h1>menú</h1>
      
    </div>
  );
};

export default menu;
